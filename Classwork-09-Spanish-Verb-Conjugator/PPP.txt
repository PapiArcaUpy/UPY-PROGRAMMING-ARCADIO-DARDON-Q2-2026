# INPUT

START

Display "Enter a verb:"
Read verb

# PROCESS

Create the list of pronouns

Create the endings for verbs ending in "ar"
Create the endings for verbs ending in "er"
Create the endings for verbs ending in "ir"

stem ← verb without the last two letters
verbType ← last two letters of the verb

If verbType = "ar"
    Use the "ar" endings
Else If verbType = "er"
    Use the "er" endings
Else
    Use the "ir" endings
End If

For i ← 0 To 5
    Display pronoun[i], stem, ending[i]
End For

# OUTPUT

END
#AI DECLARATION I USE IT TO HELP ME WITH MY CODE
